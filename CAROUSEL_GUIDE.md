# 启动轮播图功能说明

## 功能概述

启动轮播图是应用启动时显示的全屏图片轮播功能,可用于展示欢迎界面、产品介绍或重要公告。

## 功能特点

- ✨ 全屏显示轮播图片
- 🔄 自动切换图片
- 📊 底部进度条显示总进度
- 🎯 指示器显示当前页面
- ⏱️ 可配置切换时间和总显示时长
- 🖱️ 点击指示器快速跳转

## 配置方法

在 `configs/config.json` 文件中配置 `carousel` 部分:

```json
{
  "carousel": {
    "images": [
      "assets/carousel/slide1.jpg",
      "assets/carousel/slide2.jpg",
      "assets/carousel/slide3.jpg"
    ],
    "switchTime": 3000,
    "displayDuration": 15000
  }
}
```

### 配置参数说明

| 参数 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `images` | Array | 轮播图片路径数组 | `[]` |
| `switchTime` | Number | 每张图片显示时间(毫秒) | `3000` |
| `displayDuration` | Number | 轮播总显示时长(毫秒) | `15000` |

## 图片准备

1. 在 `assets/carousel/` 目录下放置图片文件
2. 支持格式: JPG, PNG, GIF
3. 推荐尺寸: 1920x1080 (16:9)
4. 文件命名建议: slide1.jpg, slide2.jpg, slide3.jpg

## 使用示例

### 示例 1: 基础轮播(3张图,每张3秒,总共15秒)

```json
{
  "carousel": {
    "images": [
      "assets/carousel/welcome.jpg",
      "assets/carousel/feature1.jpg",
      "assets/carousel/feature2.jpg"
    ],
    "switchTime": 3000,
    "displayDuration": 15000
  }
}
```

### 示例 2: 快速轮播(5张图,每张2秒,总共10秒)

```json
{
  "carousel": {
    "images": [
      "assets/carousel/slide1.jpg",
      "assets/carousel/slide2.jpg",
      "assets/carousel/slide3.jpg",
      "assets/carousel/slide4.jpg",
      "assets/carousel/slide5.jpg"
    ],
    "switchTime": 2000,
    "displayDuration": 10000
  }
}
```

### 示例 3: 禁用轮播图

```json
{
  "carousel": {
    "images": [],
    "switchTime": 3000,
    "displayDuration": 15000
  }
}
```

## 用户交互

- **自动播放**: 轮播图会自动切换,无需用户操作
- **手动切换**: 点击底部圆点指示器可跳转到指定页面
- **进度显示**: 底部进度条实时显示总播放进度
- **自动关闭**: 达到设定时长后自动关闭,进入主界面

## 注意事项

1. 如果 `images` 数组为空,将跳过轮播图直接进入主界面
2. 确保图片路径正确,否则会显示空白
3. 图片文件大小建议不超过 2MB,以确保加载速度
4. `displayDuration` 应该大于等于 `switchTime * images.length`

## 效果预览

启动应用后将看到:
- 全屏轮播图占据整个窗口
- 图片之间淡入淡出切换效果
- 底部有圆点指示器显示当前位置
- 底部有进度条显示整体进度
- 达到设定时间后自动淡出,进入应用主界面
